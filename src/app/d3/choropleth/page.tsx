import { FeatureCollection } from "geojson";
import React from "react";

type MapProps = {
  width: number;
  height: number;
  data: FeatureCollection;
};

const page = () => {
  return <div>page</div>;
};

export default page;
